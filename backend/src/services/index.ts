/**
 * Service layer placeholder.
 *
 * Domain services (video, channel, user, search, etc.) will be added here
 * when the database is connected. For now this directory exists so the
 * backend structure is ready for future implementation.
 */

export {};
